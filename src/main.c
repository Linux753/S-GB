#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "emul.h"
#include "cpu.h"

int main(int argc, char ** argv){
    srand(time(NULL));

    printf("Not enough implemented to show something, run test/ut to run unit test\n");

    return EXIT_SUCCESS;
}